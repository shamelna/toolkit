rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access is via Firebase Admin SDK (server-side only)
    // Client-side access is blocked entirely
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
